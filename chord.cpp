#include "simulator.h"
int main()
{
    // calling simulator class 
    simulator s;

    // Run Header display function to run the chord model
    s.headerDisplay(); 

    //  Simulator functions to display all the options or the commands
    s.runSimulator();
}